services:
  - type: web
    name: reflex-control-room
    runtime: static
    rootDir: reflex/frontend/Frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist

    routes:
      - type: rewrite
        source: /*
        destination: /index.html