services:
  - type: web
    name: reflex-control-room
    env: node
    buildCommand: npm install && cd artifacts/reflex-control-room && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000